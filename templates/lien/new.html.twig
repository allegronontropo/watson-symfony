{% extends 'base.html.twig' %}

{% block title %}Ajouter un lien{% endblock %}

{% block body %}
    <h1>Ajouter un lien</h1>

    <p>Remplis les champs ci-dessous pour enregistrer un nouveau lien.</p>

    {{ include('lien/_form.html.twig', { button_label: 'Créer' }) }}
    
    <br>
    
    <a href="{{ path('app_lien_index') }}" class="btn btn-secondary">
        ← Retour à la liste
    </a>
{% endblock %}
