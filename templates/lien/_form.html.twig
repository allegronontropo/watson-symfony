{{ form_start(form) }}

    {{ form_row(form.titre) }}
    {{ form_row(form.url) }}
    {{ form_row(form.description) }}

    <button class="btn btn-primary" type="submit">
        {{ button_label|default('Enregistrer') }}
    </button>

{{ form_end(form) }}
